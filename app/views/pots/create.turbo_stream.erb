<%= turbo_stream.append "pots", partial: "pots/pot", locals: { pot: @pot } %>

<turbo-stream action="after" target="pots_modal">
  <template>
    <script>
      document.dispatchEvent(new CustomEvent("modal:close"));
    </script>
  </template>
</turbo-stream>